{
  "name": "json-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx json-server --watch src/data/db.json  --port $PORT"
  },
  "author": "git",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
